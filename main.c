#include<stdio.h>
#include"tree.h"
#include"hash.h"
int main(){
	int cond;
	printf("1 - work with trees\n2 - work with hash tables\n");
	scanf("%d", &cond);
	if (cond == 1){//work with trees
	}
	if(cond == 2){ //work with hash tables
	}
	return 0;
}
